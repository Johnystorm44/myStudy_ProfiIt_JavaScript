массив - последовательность переменных одного типа
список - набор значений, в котром некоторое значение может встретиться больше одного раза

в массиве обязательно хранятся значения одного типа
нумерация индексов в массиве идет от нуля

классификация массивов

-одномерные и многомерные
-статические и динамические
-порядковые и ассоциативные
-данные одного типа и гетерогенные

объявления:
let massiv = new Array();
let massiv = [];

если число одно, это размерность

многомерные массивы:
let massiv = [
[1,2,3]
[4,5,6]
[7,8,9]
];

обращение к элементам:
massiv[0];
massiv[1][2];(1 - номер строки, 2  - номер столбца)

let people = [
    ["Том", 25, false]
    ["Bill, 38, true]
    ["Alice",21, false]
];

свойства -
length() - устанавливает или возвращает количество элементов в массиве
sort() - сортирует элементы массива

ассоциативные массивы
let massiv = new Map();
let massiv = new Map([
    ['key1', 'value1'],
    ['key2', 'value2'].
    ['key3', 'value3]
]); (key - наименование массива)

Перебор элементов массива
for(let i = 0; i < massiv.length;i++) - по индексу элемента
for(let elemMassiva of massiv) - непосредственно к элементу
massiv.forEaCH((elemMassiva) => {
    console.log(elemMassiva)
})

Основные операции со структурами данных
Поиск
1.Поиск экстремумов (минимума, максимума)
2.Поиск заданного значения(местоположения)
 

